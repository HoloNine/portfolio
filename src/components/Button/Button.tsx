const Button = () => {
  return (
    <button className="text-label px-sm rounded-full min-h-12 bg-black dark:bg-white text-white dark:text-black">
      Click me
    </button>
  );
};

export default Button;
