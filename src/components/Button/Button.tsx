const Button = () => {
  return (
    <button className="text-label px-32 rounded-full min-h-12 bg-black dark:bg-white text-white dark:text-black">
      Click me
    </button>
  );
};

export default Button;
